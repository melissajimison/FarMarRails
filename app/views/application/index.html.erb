<h1>All Markets</h1>
<%= link_to "I am a market", market_path%><br>
<%= link_to "I am a vendor", vendor_path %>

<ul>
	<% @markets.each do |market| %>
		<li><%= link_to market.name, markets_path(market.id) %> </li>
	<% end %>
</ul>
