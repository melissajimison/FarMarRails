<h1>All Markets</h1>
<%= link_to "I am a market", markets_path%><br>
<%= link_to "I am a vendor", vendors_path %>

<ul>
	<% @markets.each do |market| %>
		<li><%= link_to market.name, application_path(market.id) %> </li>
	<% end %>
</ul>
